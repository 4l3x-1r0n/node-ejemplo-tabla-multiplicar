# Notas:

Ayuda del programa

```
Options:
      --help         Show help                                             [boolean]
      --version      Show version number                                   [boolean]
  -b, --base         Base de la tabla a multiplicar              [number] [required]
  -m, --maxima-base  Hasta que base quiere multiplicar       [number] [default: "1"]
  -h, --hasta        Hasta que numero quiere multiplicar    [number] [default: "10"]
  -l, --listar       Muestra la tabla en la consola       [boolean] [default: false]
```
